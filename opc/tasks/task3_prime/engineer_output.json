{
  "files": [
    {
      "path": "prime.py",
      "content": "import math\n\ndef is_prime(n):\n    if n <= 1:\n        return False\n    if n == 2:\n        return True\n    if n % 2 == 0:\n        return False\n    for i in range(3, int(math.isqrt(n)) + 1, 2):\n        if n % i == 0:\n            return False\n    return True\n"
    }
  ],
  "summary": "创建 prime.py 文件，实现 is_prime 函数，处理 n<=1 返回 False，n==2 返回 True，对大于2的偶数直接返回 False，奇数从3到 sqrt(n) 步长2进行整除判断。"
}