{
  "files": [
    {
      "path": "flask_app/app.py",
      "content": "from flask import Flask, jsonify\n\napp = Flask(__name__)\n\n@app.route('/')\ndef home():\n    return 'Hello from Flask'\n\n@app.route('/health')\ndef health():\n    return jsonify({\"status\": \"ok\"})\n\nif __name__ == '__main__':\n    app.run(host='0.0.0.0', port=5000)\n"
    }
  ],
  "summary": "Created flask_app/app.py with the Flask application. Ensure you have run 'uv add flask' to install Flask dependency and created the flask_app directory (mkdir -p flask_app) before running 'uv run python flask_app/app.py'."
}